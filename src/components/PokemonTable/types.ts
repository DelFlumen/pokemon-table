export interface Pokemon {
  name: string;
  url: string;
}

export interface PokemonRow {
  id: number;
  name: string;
}

export interface PokemonDetails {
  id: number;
  name: string;
  height: number;
  weight: number;
}
